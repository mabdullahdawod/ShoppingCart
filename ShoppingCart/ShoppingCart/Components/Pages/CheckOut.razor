@page "/CheckOut"
@using ShoppingCart.Modal
@using ShoppingCart.SharedService
@inject ProductShareService sharedservice
@using ShoppingCart.Services
@rendermode InteractiveServer
@inject ProductServices productservice
@using ShoppingCart.Components.Pages
@inject NavigationManager navigation


<h3>CheckOut</h3>

       <div class="container">
                <form>
                    <div class="mb-3">
                        <label for="name" class="form-label">Enter your name</label>
                        <input type="text" class="form-control" id="name" placeholder="Enter name">
                    </div>

                    <div class="mb-3">
                        <label for="city" class="form-label">Select your city</label>
                        <select id="city" class="form-select">
                            <option selected>Select city</option>
                            <option value="nyc">New York City</option>
                            <option value="la">Los Angeles</option>
                            <option value="chi">Chicago</option>
                            <option value="hou">Houston</option>
                        </select>
                    </div>

                    <div class="mb-3">
                        <label for="phone" class="form-label">Enter your number</label>
                        <input type="tel" class="form-control" id="phone" placeholder="(123) 456-7890">
                    </div>

                    <button class="btn btn-primary btn-lg bg-dark mb-3 me-2 border-black">
                        <i class="bi bi-cart-check"></i> Check Out
                    </button>
                </form>

                <div class="mt-3">
                    <p class="text-dark fw-semibold">Continue shopping</p>
                </div>
            </div>




@code {

}
